{
  "name": "speech-to-text",
  "description": "",
  "version": "0.0.1",
  "author": "Hai Bacti",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node speech.js",
    "dev": "%NODE_PATH%/.bin/cross-env NODE_ENV=development %NODE_PATH%/.bin/webpack-dev-server -d --devtool source-map --port 8081 --host 0.0.0.0",
    "release": "%NODE_PATH%/.bin/cross-env NODE_ENV=production %NODE_PATH%/.bin/webpack --progress --profile --colors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google-cloud/speech": "^2.3.1"
  }
}
